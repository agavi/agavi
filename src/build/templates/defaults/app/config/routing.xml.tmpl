<?xml version="1.0" encoding="UTF-8"?>
<configurations xmlns="http://agavi.org/agavi/1.0/config">
	<configuration>
		<routes>
			<!-- This shows the welcome page after creating a project. Remove the
			     route and action once you're done admiring its shininess. -->
			<route pattern="" module="%actions.default_module%" action="Welcome" />
			
			<route pattern="^/$" module="%actions.default_module%" action="%actions.default_action%" />
		</routes>
	</configuration>
</configurations>
